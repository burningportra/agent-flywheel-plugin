node_modules/
# mcp-server/dist/ is committed so the plugin works immediately after /plugin install
# (see .github/workflows/ci.yml "dist-drift" job which prevents stale artifacts).
.pi-flywheel/checkpoint.json.tmp
.pi-flywheel/checkpoint.json.corrupt
*.log
.DS_Store
/.beads
/.pi-flywheel
/.claude/worktrees
/.hermes
/.vite

# bv (beads viewer) local config and caches
.bv/

# Vitest coverage output
mcp-server/coverage/
mcp-server/.lintskill-baseline.json

# Local runtime state
storage.sqlite3
storage.sqlite3-shm
storage.sqlite3-wal
/.pi/suggester
*.map

# Local runtime/session state — per-machine, not shipped
/.claude/
/.ntm/
/.pi-orchestrator/
/.simplify-ledger/
/refactor/

# Vendored dev copy of mcp-agent-mail (not part of the plugin distribution)
/mcp_agent_mail/

# Duel-skill scratch transcripts (kept on disk for 7-day Provenance window per
# `skills/flywheel-duel/SKILL.md`; the synthesis report at `docs/duels/<date>.md`
# captures the durable summary).
/WIZARD_IDEAS_*.md
/WIZARD_SCORES_*.md
/WIZARD_REACTIONS_*.md
/WIZARD_REBUTTAL_*.md
/WIZARD_STEELMAN_*.md
/WIZARD_BLINDSPOTS_*.md
/DUELING_WIZARDS_REPORT.md
